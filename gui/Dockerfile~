# 1. Use a Node.js base image
FROM node:18

# 2. Set the working directory
WORKDIR /app

# 3. Copy package.json and package-lock.json to the container
COPY package*.json ./

# 4. Install dependencies (only for production in this case)
RUN npm install
RUN npm install vuex@next vue-router --save

# 5. Copy all app files to the container
COPY . .

# Expose port 80 to access the app
EXPOSE 8080

# 7. Start the development server
CMD ["npm", "run", "serve"]
